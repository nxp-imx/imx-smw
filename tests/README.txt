To run tests:
$ cd /usr/local/share/smw/tests
$ ctest (or ctest --verbose to print messages)


Tree:
config/ : Directory containing config file
engine/ : Direcotry containing test engine sources files
scripts/ : Direcotry containing scripts run by ctest
vectors/ : Direcotry containing tests vectors JSON format

