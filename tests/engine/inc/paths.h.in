/* SPDX-License-Identifier: BSD-3-Clause */
/*
 * Copyright 2020-2021, 2024 NXP
 */

#ifndef __PATHS_H__
#define __PATHS_H__

/* Directory where tests status files are located */
#define DEFAULT_OUT_STATUS_PATH "${DEFAULT_OUT_STATUS_DIR}/"

/* Directory where configuration files are located */
#define CONFIG_DIR "${CMAKE_INSTALL_PREFIX}/${SMW_TESTS_TARGET_CONFIG_DIR}/"

/* Directory where CST files are located */
#define CST_KEYS_DIR "${CMAKE_INSTALL_PREFIX}/${TARGET_CST_KEYS_DIR}"
#define CST_WORKING_DIR                                                        \
	"${CMAKE_INSTALL_PREFIX}/${SMW_TESTS_TARGET_CST_TEMPLAGE_DIR_FULL_PATH}"

#endif /* __PATHS_H__ */
